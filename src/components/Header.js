import { Fragment } from "react";

const Header = () => {
  return (
    <Fragment>
      <header className="header justify-content-center">
        <h1>WeatherApp</h1>
      </header>
    </Fragment>
  );
};

export default Header;
